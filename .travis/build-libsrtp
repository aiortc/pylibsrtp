#!/bin/sh

set -e

if [ -e "libsrtp" ]; then
    make -C libsrtp distclean
else
    git clone https://github.com/cisco/libsrtp/
fi
rm -rf /tmp/libsrtp

cd libsrtp
./configure --prefix=/tmp/libsrtp
make
make install
